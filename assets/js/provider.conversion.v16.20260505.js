(() => { const PROVIDERS = [{"slug": "queenbee", "name": "여왕벌", "code": "SEOA", "href": "/consult/queenbee/", "summary": "SEOA 코드와 USDT 수수료 지원, 쿠폰 지급 조건을 가입 전 확인하는 업체입니다."}, {"slug": "skholdings", "name": "SK 홀딩스", "code": "IRON888", "href": "/consult/sk-holdings/", "summary": "IRON888 코드와 신규 입금 플러스, 스포츠·미니게임·슬롯 이벤트 조건을 확인하는 업체입니다."}, {"slug": "anybet", "name": "ANY BET", "code": "SEOA", "href": "/consult/anybet/", "summary": "SEOA 코드와 원화/테더 입금 이벤트, 스포츠·슬롯 조건을 구분해서 확인하는 업체입니다."}, {"slug": "udt", "name": "UDT", "code": "SEOA", "href": "/consult/udt/", "summary": "SEOA 코드와 슬롯 첫충/매충, 스포츠·미니게임 이벤트 조건을 확인하는 업체입니다."}, {"slug": "ddangkong", "name": "땅콩", "code": "SEOA", "href": "/consult/ddangkong/", "summary": "SEOA 코드와 BTI 스포츠, 카지노&슬롯 콤프, 주간 페이백, USDT 입금 조건을 확인하는 업체입니다."}]; const track = (name, params = {}) => { try { if (typeof window.gtag === 'function') { window.gtag('event', name, { event_category:'provider_conversion', page_path:location.pathname, ...params }); } } catch (_) {} }; const h = (v) => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c])); const inferProviders = () => { const path = location.pathname.toLowerCase(); const selected = []; const add = (slug) => { const p = PROVIDERS.find(x => x.slug === slug); if (p && !selected.includes(p)) selected.push(p); }; if (path.includes('queenbee') || path.includes('여왕벌')) add('queenbee'); if (path.includes('sk-holdings') || path.includes('sk-') || path.includes('iron')) add('skholdings'); if (path.includes('anybet') || path.includes('any-bet')) add('anybet'); if (path.includes('udt')) add('udt'); if (path.includes('ddangkong') || path.includes('땅콩')) add('ddangkong'); if (path.includes('casino') || path.includes('slot') || path.includes('bonus') || path.includes('payback') || path.includes('usdt')) { ['queenbee','anybet','ddangkong'].forEach(add); } if (path.includes('sports') || path.includes('baseball') || path.includes('football') || path.includes('basketball') || path.includes('volleyball')) { ['skholdings','udt','ddangkong'].forEach(add); } if (!selected.length) ['queenbee','skholdings','anybet'].forEach(add); return selected.slice(0,3); }; const addBlogCta = () => { const path = location.pathname; if (!path.startsWith('/blog/') || path === '/blog/' || document.querySelector('.blog-standard-cta-v16')) return; const target = document.querySelector('.entry-article, .pro-article__body, article'); if (!target) return; const items = inferProviders(); const html = `<section class="blog-standard-cta-v16" aria-label="상담 전 확인"> <h2>상담 전 확인할 것</h2> <p>글을 읽고 바로 가입하기보다, 코드·공식주소·이벤트 조건을 먼저 확인하세요.</p> <div class="blog-standard-cta-v16__grid"> ${items.map(p => `<a href="${h(p.href)}" data-provider="${h(p.slug)}" data-blog-provider-hub> <strong>${h(p.name)} : 허브입장</strong> <span>${h(p.code)} 코드와 조건 확인</span> </a>`).join('')} <a href="/tools/inquiry-builder/" data-blog-provider-hub><strong>문의 문구 만들기</strong><span>텔레그램 상담 문구 자동 정리</span></a> <a href="/consult-motives/" data-blog-provider-hub><strong>상담 전 확인</strong><span>불안한 조건 먼저 줄이기</span></a> </div> </section>`; target.insertAdjacentHTML('beforeend', html); }; document.addEventListener('click', (event) => { const copy = event.target.closest('[data-g-copy]'); if (copy) { const provider = copy.getAttribute('data-provider') || copy.closest('[data-provider]')?.getAttribute('data-provider') || ''; track('provider_code_copy', { provider, code: copy.getAttribute('data-g-copy') || '' }); const card = copy.closest('.gateway-card'); if (card) { card.classList.add('is-code-ready'); const open = card.querySelector('[data-g-open]'); if (open && !open.dataset.readyText) { open.dataset.readyText = '1'; open.textContent = open.textContent.replace('접속', '접속하기'); } } } const official = event.target.closest('[data-g-open], [data-provider-official]'); if (official) { const provider = official.getAttribute('data-provider') || official.closest('[data-provider]')?.getAttribute('data-provider') || ''; track('provider_official_click', { provider }); } const consult = event.target.closest('[data-provider-consult]'); if (consult) { const provider = consult.getAttribute('data-provider') || consult.closest('[data-provider]')?.getAttribute('data-provider') || ''; track('provider_consult_click', { provider }); } const blogHub = event.target.closest('[data-blog-provider-hub]'); if (blogHub) { track('blog_provider_hub_click', { href: blogHub.getAttribute('href') || '' }); } const consultOpen = event.target.closest('[data-auto-consult-open], .auto-consult-launcher'); if (consultOpen) { track('consult_open', { source: consultOpen.getAttribute('data-auto-consult-open') || 'launcher' }); } }); if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', addBlogCta); else addBlogCta();
})();
